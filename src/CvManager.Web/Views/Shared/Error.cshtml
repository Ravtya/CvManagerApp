@model ErrorViewModel
@{
    ViewData["Title"] = L["Shared_Error"];
}

<h1 class="h5 text-danger mb-2">@L["Shared_Error_Header"]</h1>
@if (Model.ShowRequestId)
{
    <p class="small text-muted mb-0">@L["Shared_Error_RequestId"]: <code>@Model.RequestId</code></p>
}
